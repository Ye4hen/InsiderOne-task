export const TOTAL_HORSES_COUNT = Number.parseInt(import.meta.env.VITE_TOTAL_HORSES_COUNT)
export const HORSES_COUNT_PER_ROUND = Number.parseInt(import.meta.env.VITE_HORSES_COUNT_PER_ROUND)
export const ROUNDS_COUNT = Number.parseInt(import.meta.env.VITE_ROUNDS_COUNT)
export const BASE_SPEED = Number.parseInt(import.meta.env.VITE_BASE_SPEED)
